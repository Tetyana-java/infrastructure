terraform init
terraform import aws_secretsmanager_secret.postgres spoonacular-dev/postgres
terraform apply -var-file=dev.tfvars

aws eks update-kubeconfig \
  --region us-east-1 \
  --name spoonacular-dev-eks \
  --profile my-user

kubectl get nodes


# Install
helm install spoonacular . -f values.yaml

# Upgrade (for new image or config)
helm upgrade spoonacular . -f values.yaml

# Uninstall
helm uninstall spoonacular

#Update credentials
aws sts get-caller-identity --profile my-user
# Restart pods to pick up new secret
kubectl rollout restart deployment spoonacular-app

Step 1 — Install CSI driver
helm repo add secrets-store-csi-driver https://kubernetes-sigs.github.io/secrets-store-csi-driver/charts
helm repo update
Step 2 — Install CRDs + driver
helm install csi-secrets-store secrets-store-csi-driver/secrets-store-csi-driver \
  --namespace kube-system
 Step 3 — Install AWS provider (IMPORTANT for AWS)
kubectl apply -f https://raw.githubusercontent.com/aws/secrets-store-csi-driver-provider-aws/main/deployment/aws-provider-installer.yaml
Verify installation
kubectl get crds | grep secretproviderclass


kubectl patch csidriver secrets-store.csi.k8s.io --type='merge' -p '{
  "spec": {
    "tokenRequests": [
      {
        "audience": "sts.amazonaws.com"
      }
    ]
  }
}'

he CSI driver was installed without syncSecret.enabled=true,
helm upgrade csi-secrets-store secrets-store-csi-driver/secrets-store-csi-driver --namespace kube-system --set syncSecret.enabled=true --set enableSecretRotation=true 2>&1 | cat

helm repo add eks https://aws.github.io/eks-charts
helm repo update

helm install aws-load-balancer-controller eks/aws-load-balancer-controller \
  -n kube-system \
  --set clusterName=spoonacular-dev-eks \
  --set serviceAccount.create=true \
  --set serviceAccount.name=aws-load-balancer-controller \
  --set serviceAccount.annotations."eks\.amazonaws\.com/role-arn"=arn:aws:iam::270770993091:role/spoonacular-dev-alb-controller-role

kubectl get ingress -n default
  

To call from POSTMAN:
kubectl port-forward svc/spoonacular-app-service 8080:80 --namespace default
POST http://localhost:8080/api/suggest-lunch

{
"characteristicsDto":{
"gender": "WOMAN",
"activityLevel": "SEDENTARY",
"goal": "LOOSE_WEIGHT",
"height": 170,
"weight": 70,
 "age": 55
    },
"cuisinePreferences" :[1,2,3],
"ingredientsExclusions": ["chocolate"]
}

Links:
https://github.com/terraform-aws-modules/terraform-aws-vpc

TO CHECK:
The ALB Ingress Controller itself must be installed on your cluster. Without it, your ingressClassName: alb does nothing. Check if it's installed:
